"use client";

/*
  v67 preview hard-fix:
  WizardSteps duplicated navigation/status over the real Studio pipeline.
  The main Studio already has one source of truth and a status bar.
  Keep this component as a safe no-op instead of deleting the file.
*/

export default function WizardSteps() {
  return null;
}
